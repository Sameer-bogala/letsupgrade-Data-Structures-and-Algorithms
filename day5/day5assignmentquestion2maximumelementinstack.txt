
Question 2 
Write a function to find the maximum element in the stack.
ans:-
void maximum()
{
	if(top==-1)
		printf(“Not possible”);
	else
	{
		int max=pop();
		for(int i=top; i>=0; i--)
		{
			int temp=pop();
			if(max<temp)
				max=temp;
		}
		printf(“%d”, max);
	}
}
